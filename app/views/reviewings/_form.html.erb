<%= form_for(@reviewing) do |f| %>
  <% if @reviewing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reviewing.errors.count, "error") %> prohibited this reviewing from being saved:</h2>

      <ul>
      <% @reviewing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :book_id, :value => params[:book_id] %>
  <%= f.hidden_field :user_id, :value => current_user.id %>
  
  <div class="field">
    <%= f.label :rate %><br>
    <%= f.text_field :rate %>
  </div>
  <div class="field">
    <%= f.label :review %><br>
    <%= f.text_area :review %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
